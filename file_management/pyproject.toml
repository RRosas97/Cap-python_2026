[project]
name = "file-management"
version = "0.1.0"
description = ""
authors = [
    {name = "Richier9",email = "ricardorosas32@aragon.unam.mx"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "file_management", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
