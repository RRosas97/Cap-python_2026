[project]
name = "second-module"
version = "0.1.0"
description = ""
authors = [
    {name = "RRosas97",email = "ricardo.rosas@axity.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "second_module", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
