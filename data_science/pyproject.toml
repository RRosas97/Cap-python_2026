[project]
name = "data-science"
version = "0.1.0"
description = ""
authors = [
    {name = "Richier9",email = "ricardo.rosas@axity.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy (>=2.5.2,<3.0.0)",
    "polars (>=1.44.0,<2.0.0)",
    "pandas (>=3.0.5,<4.0.0)",
    "joblib (>=1.5.3,<2.0.0)",
    "scikit-learn (>=1.9.0,<2.0.0)"
]

[tool.poetry]
packages = [{include = "data_science", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
